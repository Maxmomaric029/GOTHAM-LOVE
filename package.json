{
  "name": "maximista-mod-menu",
  "version": "1.0.0",
  "description": "Premium Mod Menu APK with Floating Overlay",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Building web content...' && mkdir -p www && cp index.html www/ && cp style.css www/ && cp script.js www/",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
